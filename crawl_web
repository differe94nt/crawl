def crawl_web(seed):
    tocrawl=[seed]
    crawled=[]
    while tocrawl:
        url=tocrawl.pop()
        page=get_page(url)
        tocrawl+=get_links(page)
        crawled+=url
    return crawled
print crawl_web('http://msn.com')
